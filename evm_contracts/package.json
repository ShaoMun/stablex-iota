{
  "name": "stablex-evm-contracts",
  "version": "1.0.0",
  "description": "StableX EVM contracts for IOTA EVM testnet",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy": "hardhat run scripts/deploy.js --network iota-evm-testnet",
    "deploy:testnet": "hardhat run scripts/deploy-testnet.js --network iota-evm-testnet"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "hardhat": "^2.19.0"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.0.0",
    "ethers": "^6.15.0"
  }
}
